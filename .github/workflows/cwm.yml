name: Build CWM/PhilZ Recovery (Legacy)

on:
  workflow_dispatch:
    inputs:
      MANIFEST_URL:
        description: 'Manifest 仓库 URL (CM-10.1 源码)'
        required: true
        default: 'https://github.com/CyanogenMod/android.git'
      MANIFEST_BRANCH:
        description: 'Manifest 分支 (cm-10.1)'
        required: true
        default: 'cm-10.1'
      DEVICE_TREE_URL:
        description: '你的设备树仓库 URL'
        required: true
        default: 'https://github.com/Xxinn034/android_device_ireadygo_G2'
      DEVICE_TREE_BRANCH:
        description: '设备树分支'
        required: true
        default: 'main'
      DEVICE_PATH:
        description: '设备树路径 (如: device/ireadygo/G2)'
        required: true
        default: 'device/ireadygo/G2'
      DEVICE_NAME:
        description: '设备代号'
        required: true
        default: 'G2'
      MAKEFILE_NAME:
        description: '编译目标 (如: cm_G2-eng)'
        required: true
        default: 'cm_G2-eng'

jobs:
  build:
    runs-on: ubuntu-18.04  # 编译 Android 4.x 必须使用 Ubuntu 18.04
    permissions:
      contents: write

    steps:
    - name: Check Out
      uses: actions/checkout@v4

    - name: Cleanup
      uses: rokibhasansagar/slimhub_actions@main

    - name: Prepare Environment
      run: |
        sudo apt update && sudo apt upgrade -y
        DEBIAN_FRONTEND=noninteractive sudo apt install -yq \
          gperf gcc-multilib g++-multilib libc6-dev lib32ncurses5-dev \
          x11proto-core-dev libx11-dev tree lib32z-dev libgl1-mesa-dev \
          libxml2-utils xsltproc bc ccache lib32readline-dev lib32z1-dev \
          liblz4-tool libncurses5-dev libsdl1.2-dev libwxgtk3.0-gtk3-dev \
          libxml2 lzop pngcrush schedtool squashfs-tools imagemagick \
          libbz2-dev lzma ncftp qemu-user-static libstdc++-10-dev \
          python3 tar git-lfs m4

    - name: Install OpenJDK 8
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '8'

    - name: Install repo
      run: |
        mkdir ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo

    - name: Initialize repo
      id: pwd
      run: |
        mkdir workspace
        cd workspace
        echo "workspace-folder=$(pwd)" >> $GITHUB_OUTPUT
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        repo init --depth=1 -u ${{ github.event.inputs.MANIFEST_URL }} \
          -b ${{ github.event.inputs.MANIFEST_BRANCH }}

    - name: Repo Sync
      run: repo sync -j$(nproc --all) --force-sync
      working-directory: workspace

    - name: Clone Device Tree
      run: |
        git clone ${{ github.event.inputs.DEVICE_TREE_URL }} \
          -b ${{ github.event.inputs.DEVICE_TREE_BRANCH }} \
          ./${{ github.event.inputs.DEVICE_PATH }}
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Replace Recovery Source with PhilZ Touch
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}
        # 删除原有的 CWM 源码
        rm -rf bootable/recovery
        # 克隆 PhilZ Touch 源码到正确位置[reference:5]
        git clone https://github.com/PhilZ-cwm6/philz_touch_cwm6 bootable/recovery -b cm-10.1

    - name: Set Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 12

    - name: Build Recovery
      id: build
      continue-on-error: true
      run: |
        cd ${{ steps.pwd.outputs.workspace-folder }}
        
        # 加载构建环境
        source build/envsetup.sh
        lunch ${{ github.event.inputs.MAKEFILE_NAME }}
        
        # 清理并编译 recovery 镜像
        make clean
        make recoveryimage V=1 -k -j$(nproc --all) 2>&1 | tee build.log
        
        # 保存构建状态
        if [ ${PIPESTATUS[0]} -eq 0 ]; then
          echo "build_status=success" >> $GITHUB_OUTPUT
        else
          echo "build_status=failure" >> $GITHUB_OUTPUT
        fi

    - name: Print build errors (if any)
      if: steps.build.outputs.build_status == 'failure'
      run: |
        echo "========== Build failed, printing error excerpts =========="
        cd ${{ steps.pwd.outputs.workspace-folder }}
        grep -i "error:" build.log | head -n 20 || echo "No error lines found."
        echo "========== Last 50 lines of build.log =========="
        tail -n 50 build.log

    - name: Upload to Release
      if: steps.build.outputs.build_status == 'success'
      uses: softprops/action-gh-release@v2
      with:
        files: |
          workspace/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/recovery.img
        name: ${{ github.event.inputs.DEVICE_NAME }}-CWM-${{ github.run_id }}
        tag_name: ${{ github.run_id }}
        body: |
          Branch: ${{ github.event.inputs.MANIFEST_BRANCH }}
          Device: ${{ github.event.inputs.DEVICE_NAME }}
          Recovery: PhilZ Touch / CWM
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload build log (always)
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: build-log
        path: ${{ steps.pwd.outputs.workspace-folder }}/build.log