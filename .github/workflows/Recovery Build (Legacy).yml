name: Recovery Build (Legacy)

on:
  workflow_dispatch:
    inputs:
      MANIFEST_URL:
        description: 'Manifest仓库URL（通常无需修改）'
        required: true
        default: 'https://github.com/minimal-manifest-twrp/platform_manifest_twrp_omni'
      MANIFEST_BRANCH:
        description: 'Manifest分支（Android 8.1请使用twrp-8.1）'
        required: true
        default: 'twrp-8.1'
      DEVICE_TREE_URL:
        description: '你的设备树仓库URL'
        required: true
        default: 'https://github.com/你的用户名/你的设备树仓库'
      DEVICE_TREE_BRANCH:
        description: '设备树分支'
        required: true
        default: 'main'
      DEVICE_PATH:
        description: '设备树在源码中的路径（如 device/厂商/代号）'
        required: true
        default: 'device/你的厂商/你的代号'
      DEVICE_NAME:
        description: '设备代号（将用于输出文件名）'
        required: true
        default: '你的设备代号'
      MAKEFILE_NAME:
        description: '编译目标名称（通常为 omni_你的设备代号）'
        required: true
        default: 'omni_你的设备代号'
      BUILD_TARGET:
        description: '编译目标（recovery或boot）'
        required: true
        default: 'recovery'

jobs:
  build:
    if: github.event.repository.owner.id == github.event.sender.id
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
    - name: Display Run Parameters
      run: |
        echo "::group::User Environment Variables"
        echo "Manifest URL: ${{ github.event.inputs.MANIFEST_URL }}"
        echo "Manifest Branch: ${{ github.event.inputs.MANIFEST_BRANCH }}"
        echo "Device Tree URL: ${{ github.event.inputs.DEVICE_TREE_URL }}"
        echo "Device Tree Branch: ${{ github.event.inputs.DEVICE_TREE_BRANCH }}"
        echo "Device Path: ${{ github.event.inputs.DEVICE_PATH }}"
        echo "Device Name: ${{ github.event.inputs.DEVICE_NAME }}"
        echo "Makefile Name: ${{ github.event.inputs.MAKEFILE_NAME }}"
        echo "Build Target: ${{ github.event.inputs.BUILD_TARGET }}.img"
        echo "::endgroup::"

    - name: Check Out
      uses: actions/checkout@v4

    - name: Cleanup
      uses: rokibhasansagar/slimhub_actions@main

    - name: Prepare the environment
      run: |
        sudo apt update && sudo apt upgrade -y
        DEBIAN_FRONTEND=noninteractive sudo apt install -yq \
            linux-modules-extra-$(uname -r) \
            gperf gcc-multilib gcc-10-multilib g++-multilib g++-10-multilib \
            libc6-dev lib32ncurses5-dev x11proto-core-dev libx11-dev tree lib32z-dev libgl1-mesa-dev libxml2-utils \
            xsltproc bc ccache lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev \
            libwxgtk3.0-gtk3-dev libxml2 lzop pngcrush schedtool squashfs-tools \
            imagemagick libbz2-dev lzma ncftp qemu-user-static libstdc++-10-dev libncurses5 python3 tar

    - name: Install OpenJDK 8
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '8'

    - name: Setup SSH Keys
      if: ${{ startsWith(github.event.inputs.MANIFEST_URL, 'git@github.com') || 
          startsWith(github.event.inputs.DEVICE_TREE_URL, 'git@github.com') }}
      uses: webfactory/ssh-agent@v0.9.0
      with:
          ssh-private-key: |
            ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Install repo
      run: |
        mkdir ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo

    - name: Initialize repo
      run: |
        mkdir workspace
        cd workspace
        echo "workspace-folder=$(pwd)" >> $GITHUB_OUTPUT
        git config --global user.name "azwhikaru"
        git config --global user.email "azwhikaru+37921907@github.com"
        repo init --depth=1 -u ${{ github.event.inputs.MANIFEST_URL }} -b ${{ github.event.inputs.MANIFEST_BRANCH }}
      id: pwd

    - name: Repo Sync
      run: |
        repo sync -j$(nproc --all) --force-sync
      working-directory: workspace

    # ========== 修复 checkpolicy 多重定义 ==========
    - name: Fix checkpolicy multiple definition
      run: |
        set -e
        CHECKPOLICY_H="external/checkpolicy/checkpolicy.h"
        POLICY_DEFINE="external/checkpolicy/policy_define.c"
        sed -i '/^int te_assertions = 0;/d' "$POLICY_DEFINE"
        if grep -q "^te_assert_t \*te_assertions;" "$CHECKPOLICY_H"; then
          sed -i 's/^te_assert_t \*te_assertions;/extern te_assert_t *te_assertions;/' "$CHECKPOLICY_H"
        fi
        if ! grep -q "^te_assert_t \*te_assertions =" "$POLICY_DEFINE"; then
          echo "te_assert_t *te_assertions = NULL;" >> "$POLICY_DEFINE"
        fi
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    # ========== 修复 libselinux ==========
    - name: Fix libselinux static libs
      run: |
        MK="external/libselinux/Android.mk"
        if [ -f "$MK" ]; then
          sed -i '/LOCAL_WHOLE_STATIC_LIBRARIES/d' "$MK"
          sed -i '/LOCAL_STATIC_LIBRARIES/d' "$MK"
          echo "LOCAL_STATIC_LIBRARIES := libpcre" >> "$MK"
        fi
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Clone device tree
      run: |
        git clone ${{ github.event.inputs.DEVICE_TREE_URL }} -b ${{ github.event.inputs.DEVICE_TREE_BRANCH }} ./${{ github.event.inputs.DEVICE_PATH }}
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    # ========== 创建符号链接（仅用于兼容，不再复制文件） ==========
    - name: Create symlink for compatibility
      run: |
        mkdir -p device/gionee
        TARGET_PATH="${{ github.event.inputs.DEVICE_PATH }}"
        LINK_PATH="device/gionee/GiONEE_BFL7506A"
        if [ ! -e "$LINK_PATH" ]; then
          ln -sf "../${TARGET_PATH#device/}" "$LINK_PATH"
        fi
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Remove incompatible Android.bp
      run: |
        if [ -f ./${{ github.event.inputs.DEVICE_PATH }}/Android.bp ]; then
          rm ./${{ github.event.inputs.DEVICE_PATH }}/Android.bp
        fi
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    # ========== 强制精简 BoardConfig ==========
    - name: Force TWRP size optimizations
      run: |
        BOARD_FILE="${{ steps.pwd.outputs.workspace-folder }}/${{ github.event.inputs.DEVICE_PATH }}/BoardConfig.mk"
        if [ ! -f "$BOARD_FILE" ]; then
          echo "❌ BoardConfig.mk not found! Exiting."
          exit 1
        fi
        sed -i '/BOARD_RAMDISK_COMPRESSOR/d' "$BOARD_FILE"
        echo "BOARD_RAMDISK_COMPRESSOR := xz" >> "$BOARD_FILE"
        for flag in TW_INCLUDE_BASH TW_INCLUDE_NANO TW_INCLUDE_PIGZ TW_INCLUDE_OPENAES TW_INCLUDE_TWRPAPP TW_INCLUDE_CRYPTO TW_INCLUDE_BUSYBOX TW_INCLUDE_OPENSSH; do
          sed -i "/$flag/d" "$BOARD_FILE"
          echo "$flag := false" >> "$BOARD_FILE"
        done
        sed -i '/TW_EXCLUDE_MTP/d' "$BOARD_FILE"
        sed -i '/TW_EXTRA_LANGUAGES/d' "$BOARD_FILE"
        echo "TW_EXTRA_LANGUAGES := true" >> "$BOARD_FILE"
        sed -i '/TW_DEFAULT_LANGUAGE/d' "$BOARD_FILE"
        echo "TW_DEFAULT_LANGUAGE := zh_CN" >> "$BOARD_FILE"
        sed -i '/TW_DEVICE_VERSION/d' "$BOARD_FILE"
        echo "TW_DEVICE_VERSION := Xxinn034" >> "$BOARD_FILE"
        echo "BOARD_RECOVERYIMAGE_PARTITION_SIZE := 20971520" >> "$BOARD_FILE"
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    # ========== 【最终修复】强制创建产品配置文件（无复制） ==========
    - name: Force correct product makefiles
      run: |
        WORKSPACE="${{ steps.pwd.outputs.workspace-folder }}"
        DEVICE_DIR="$WORKSPACE/${{ github.event.inputs.DEVICE_PATH }}"
        PRODUCT_NAME="${{ github.event.inputs.MAKEFILE_NAME }}"
        DEVICE_NAME="${{ github.event.inputs.DEVICE_NAME }}"

        echo "Creating product makefiles in $DEVICE_DIR"
        # 1. 创建 AndroidProducts.mk，使用 $(LOCAL_DIR) 相对路径
        cat > "$DEVICE_DIR/AndroidProducts.mk" <<EOF
        PRODUCT_MAKEFILES := \\
            \$(LOCAL_DIR)/$PRODUCT_NAME.mk
        EOF

        # 2. 创建 PRODUCT_NAME.mk
        cat > "$DEVICE_DIR/$PRODUCT_NAME.mk" <<EOF
        \$(call inherit-product, build/target/product/omni.mk)
        PRODUCT_NAME := $PRODUCT_NAME
        PRODUCT_DEVICE := $DEVICE_NAME
        PRODUCT_BRAND := GiONEE
        PRODUCT_MODEL := BFL7506A
        PRODUCT_MANUFACTURER := GiONEE
        PRODUCT_PACKAGES += recovery twrp
        \$(call inherit-product, vendor/omni/config/gsm.mk)
        EOF

        # 3. 在 BoardConfig.mk 设置 TARGET_DEVICE（使用设备代号）
        sed -i '/^TARGET_DEVICE/d' "$DEVICE_DIR/BoardConfig.mk"
        echo "TARGET_DEVICE := $DEVICE_NAME" >> "$DEVICE_DIR/BoardConfig.mk"

        # 4. 更新 vendorsetup.sh
        VENDOR_SETUP="$DEVICE_DIR/vendorsetup.sh"
        if [ -f "$VENDOR_SETUP" ]; then
          sed -i "/add_lunch_combo/d" "$VENDOR_SETUP"
        fi
        echo "add_lunch_combo $PRODUCT_NAME-eng" >> "$VENDOR_SETUP"

        echo "✅ Product files created successfully"
        # 列出文件供调试
        ls -l "$DEVICE_DIR"
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}
    # ==============================================================

    # 清理 device.mk 中的引用（辅助）
    - name: Clean device.mk references
      run: |
        DEVICE_DIR="${{ steps.pwd.outputs.workspace-folder }}/${{ github.event.inputs.DEVICE_PATH }}"
        for mkfile in device.mk omni.mk; do
          if [ -f "$DEVICE_DIR/$mkfile" ]; then
            sed -i '/busybox/d' "$DEVICE_DIR/$mkfile"
            sed -i '/bash/d' "$DEVICE_DIR/$mkfile"
            sed -i '/nano/d' "$DEVICE_DIR/$mkfile"
            sed -i '/pigz/d' "$DEVICE_DIR/$mkfile"
            sed -i '/openaes/d' "$DEVICE_DIR/$mkfile"
            sed -i '/twrpapp/d' "$DEVICE_DIR/$mkfile"
            sed -i '/TWRPApp/d' "$DEVICE_DIR/$mkfile"
            sed -i '/libncurses/d' "$DEVICE_DIR/$mkfile"
            sed -i '/openssh/d' "$DEVICE_DIR/$mkfile"
          fi
        done

    # 语言精简
    - name: Prune non-Chinese languages
      run: |
        LANG_DIR="${{ steps.pwd.outputs.workspace-folder }}/bootable/recovery/gui/theme/extra-languages/"
        if [ -d "$LANG_DIR" ]; then
          cd "$LANG_DIR"
          for lang in $(ls -d */ 2>/dev/null | grep -v -E '^(fonts|languages|zh_CN)/'); do
            rm -rf "$lang"
          done
          cd - >/dev/null
        fi

    - name: Set Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 12

    - name: Switch to Python 2
      run: |
        sudo apt-get install -y python2
        wget https://bootstrap.pypa.io/pip/2.7/get-pip.py
        python2 get-pip.py
        sudo rm -rf /usr/bin/python
        sudo ln -s /usr/bin/python2 /usr/bin/python

    - name: Building recovery
      run: |
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        lunch ${{ github.event.inputs.MAKEFILE_NAME }}-eng && make clean && make ${{ github.event.inputs.BUILD_TARGET }}image -j$(nproc --all)
      working-directory: ${{ steps.pwd.outputs.workspace-folder }}

    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: | 
          workspace/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/${{ github.event.inputs.BUILD_TARGET }}.img
          workspace/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/*.zip
          workspace/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/*vendor*.img
        name: ${{ github.event.inputs.DEVICE_NAME }}-${{ github.run_id }}
        tag_name: ${{ github.run_id }}
        body: |
          Manifest: ${{ github.event.inputs.MANIFEST_BRANCH }}
          Device: ${{ github.event.inputs.DEVICE_NAME }}
          Target: ${{ github.event.inputs.BUILD_TARGET }}.img
          Build by Xxinn034 with TWRP customizations
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
